const config = {
    webhook: "Webhook goes here",
    time: 10, // in min

    connection: {
        host: "localhost",
        user: "root",
        password: "",
        database: "fivem",
    },

    embed: {
        color: "BLACK"
    }
}

module.exports = config